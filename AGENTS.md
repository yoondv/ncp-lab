# 프로젝트 지침

## 프로젝트 목적

- 이 프로젝트는 CI/CD 파이프라인 구축과 배포 과정을 학습하기 위한 프로젝트다.
- GitHub Actions, Docker, GitHub Container Registry(GHCR), Naver Cloud Platform(NCP)을 순서대로 연동할 예정이다.
- 배포 대상은 Python으로 작성한 단일 서버 파일을 기본으로 한다.

## 기술 스택

- Python 3.14.7과 FastAPI를 사용한다.
- GitHub Actions를 사용해 CI/CD 워크플로를 구성한다.
- Docker를 사용해 애플리케이션 이미지를 빌드한다.
- GitHub Container Registry(GHCR)를 사용해 Docker 이미지를 저장하고 관리한다.
- Naver Cloud Platform(NCP)을 최종 배포 환경으로 사용한다.

## 작업 진행 원칙

- 요청을 여러 방식으로 해석할 수 있거나 진행 중 요구사항을 보완해야 한다면 작업을 멈추고 사용자에게 질문한다.
- 요청의 깊이와 작업 규모에 맞춰 답변 및 작업 시간을 조절한다.
- 작업이 오래 걸릴 것으로 예상되면 작업 전에 예상 소요 시간이 길다는 점을 알린다.
- 실행에 필요한 최소한의 뼈대부터 작성한 후 요청된 기능을 단계적으로 추가한다.
- 사용자가 요청한 코드만 추가하며, 명시되지 않은 기능을 임의로 추측하여 구현하지 않는다.
- 짧은 코드로 구현할 수 있거나 의존성 그래프가 깊어지는 작업은 구현 전에 가능한 방법을 제시한다.

## 코드 및 파일 관리

- 요구사항을 충족하는 범위에서 항상 효율적이고 최적화된 코드를 작성한다.
- 코드를 작성하는 동안 동일한 기능을 더 단순하고 가볍게 구현할 수 있는지 확인한다.
- 파일 구조는 최대한 단순하게 유지하되, 실행과 배포에 필수적인 파일은 포함한다.
- 작업 중 사용되지 않는 변수, 함수 또는 파일을 발견하면 사용자에게 알린다.
- 환경 변수는 사용자만 작성하고 관리한다. 환경 변수를 임의로 생성하거나 수정하지 않는다.

## 보안

- 보안 취약점이 발생했거나 요청한 작업이 보안 취약점을 유발할 수 있다면 모든 작업을 즉시 중단하고 사용자에게 알린다.

## 답변 방식

- 답변에는 추가하거나 변경한 기능과 각 기능의 역할을 간단히 요약한다.
- 작업 단계나 선택 분기가 끝날 때마다 다음 목표를 제시한다.
